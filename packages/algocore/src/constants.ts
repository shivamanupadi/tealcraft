export enum TEXT_ENCODING {
  BASE64 = "base64",
  TEXT = "text",
  MSG_PACK = "msgpack",
  HEX = "hex",
  JSON = "json",
}

export enum PROGRAM_ENCODING {
  BASE64 = "base64",
  TEAL = "teal",
  TEXT = "text",
}
